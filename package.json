{
  "name": "manifest",
  "version": "4.0.0-beta",
  "scripts": {
    "build": "npx turbo run build",
    "dev": "npx turbo run dev",
    "test": "npx turbo run test"
  },
  "workspaces": [
    "packages/**/*"
  ],
  "devDependencies": {
    "turbo": "^2.1.1"
  },
  "packageManager": "npm@10.7.0"
}
