{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "dev": {
      "persistent": true,
      "cache": false
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "manifest#build": {
      "dependsOn": ["@mnfst/shared#build", "manifest-frontend#build", "manifest-backend#build"],
      "outputs": ["dist/**", "public/**", "skills/**"]
    },
    "test": {
      "cache": false
    },
    "lint": {
      "cache": false
    }
  }
}
