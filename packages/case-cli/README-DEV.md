# CASE CLI

## Develop

Watch changes:

```bash
npm run build:watch
```

Run command:

```bash
node cli.js
```

## Publish to npm

To publish, simply update the version number in `package.json` and run:

```bash
npm run build

npm publish
```
