{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://schema.manifest.build/definitions/middlewares/middlewares-schema.json",
  "title": "Middlewares Schema",
  "description": "The middlewares related to events the entity records. Doc: https://manifest.build/docs/middlewares",
  "type": "object",
  "properties": {
    "beforeCreate": {
      "type": "array",
      "description": "Middlewares to run before creating a record. Doc: https://manifest.build/docs/middlewares#events",
      "items": {
        "$ref": "middleware-schema.json"
      }
    },
    "afterCreate": {
      "type": "array",
      "description": "Middlewares to run after creating a record. Doc: https://manifest.build/docs/middlewares#events",
      "items": {
        "$ref": "middleware-schema.json"
      }
    },
    "beforeUpdate": {
      "type": "array",
      "description": "Middlewares to run before updating a record. Doc: https://manifest.build/docs/middlewares#events",
      "items": {
        "$ref": "middleware-schema.json"
      }
    },
    "afterUpdate": {
      "type": "array",
      "description": "Middlewares to run after updating a record. Doc: https://manifest.build/docs/middlewares#events",
      "items": {
        "$ref": "middleware-schema.json"
      }
    },
    "beforeDelete": {
      "type": "array",
      "description": "Middlewares to run before deleting a record. Doc: https://manifest.build/docs/middlewares#events",
      "items": {
        "$ref": "middleware-schema.json"
      }
    },
    "afterDelete": {
      "type": "array",
      "description": "Middlewares to run after deleting a record. Doc: https://manifest.build/docs/middlewares#events",
      "items": {
        "$ref": "middleware-schema.json"
      }
    }
  },
  "additionalProperties": false
}
