/**
 * Property types.
 */
export enum PropType {
  String = 'string',
  Text = 'text',
  Number = 'number',
  Link = 'link',
  Money = 'money',
  Date = 'date',
  Timestamp = 'timestamp',
  Email = 'email',
  Boolean = 'boolean',
  Password = 'password',
  Choice = 'choice',
  Location = 'location'
}
