describe('Upload (e2e)', () => {
  describe('Upload file', () => {
    it('should upload a file', () => {})

    it('should apply the validation rules from entity property', () => {})
  })

  describe('Upload image', () => {
    it('should upload an image', () => {})

    it('should return the path of the uploaded images based on the image sizes', () => {})

    it('should apply the validation rules from entity property', () => {})
  })
})
