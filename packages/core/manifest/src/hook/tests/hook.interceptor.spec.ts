import { HookInterceptor } from '../hook.interceptor'

describe('HookInterceptor', () => {
  it('should be defined', () => {
    expect(new HookInterceptor()).toBeDefined()
  })

  it('should detect hooks for events', () => {})

  it('should trigger webhooks', () => {})

  it('should trigger webhook sequentially', () => {})
})
