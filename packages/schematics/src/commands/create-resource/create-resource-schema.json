{
  "$schema": "http://json-schema.org/schema",
  "$id": "createClientResource",
  "title": "Create Client Resource Schema",
  "type": "object",
  "description": "Create a resource",
  "properties": {
    "name": {
      "type": "string",
      "minLength": 1,
      "description": "The name of the resource that will be displayed (singular, in english. e.g. 'User', 'Corporate group')"
    }
  },
  "required": ["name"]
}
