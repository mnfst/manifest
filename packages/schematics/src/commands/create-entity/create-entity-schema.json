{
  "$schema": "http://json-schema.org/schema",
  "$id": "createClientEntity",
  "title": "Create Client Entity Schema",
  "type": "object",
  "description": "Create a entity",
  "properties": {
    "name": {
      "type": "string",
      "minLength": 1,
      "description": "The name of the entity that will be displayed (singular, in english. e.g. 'User', 'Corporate group')"
    }
  },
  "required": ["name"]
}
