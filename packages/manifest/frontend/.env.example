# Backend API URL (leave empty for production/same-origin, unset for local dev)
# VITE_API_URL=http://localhost:3847

# Registry URL for UI components (optional - defaults to https://ui.manifest.build/r)
# VITE_REGISTRY_URL=https://ui.manifest.build/r
