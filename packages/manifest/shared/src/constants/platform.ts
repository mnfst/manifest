import type { PreviewPreferences } from '../types/platform.js';

/**
 * Default preview preferences
 */
export const DEFAULT_PREVIEW_PREFERENCES: PreviewPreferences = {
  platformStyle: 'chatgpt',
  themeMode: 'light',
};
