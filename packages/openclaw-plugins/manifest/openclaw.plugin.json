{
  "id": "manifest",
  "name": "Manifest — Self-Hosted LLM Router",
  "version": "5.32.0",
  "description": "Run the Manifest LLM router locally with SQLite. Zero-config dashboard included.",
  "author": "MNFST Inc.",
  "homepage": "https://manifest.build",
  "providers": ["manifest"],
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "port": {
        "type": "number",
        "default": 2099,
        "description": "Port for the local embedded server."
      },
      "host": {
        "type": "string",
        "default": "127.0.0.1",
        "description": "Bind address for the local embedded server."
      }
    }
  },
  "uiHints": {
    "port": { "label": "Server Port" },
    "host": { "label": "Bind Address" }
  }
}
