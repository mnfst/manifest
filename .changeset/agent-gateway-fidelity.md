---
'manifest': patch
---

Preserve Claude Code and Codex protocol fidelity through Manifest by forwarding safe client and session metadata only to matching upstreams, keeping native Claude Messages bodies intact, accepting streamed request bodies, retaining Claude's recognized default model for permission Auto mode, keeping Permission Auto classifier calls on Anthropic, and preserving Responses tool-call lifecycles, provider errors, and response headers across fallback conversions.
