---
"manifest": minor
---

Add deep-link URL support for provider modals on the routing page. Navigate directly to a specific provider's connect form via `?provider=<id>` (e.g. `?provider=gemini`, `?provider=anthropic`).
