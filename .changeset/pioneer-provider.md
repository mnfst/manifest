---
'manifest': patch
---

Add Pioneer as a first-class API-key provider. Manifest can now connect Pioneer keys, discover models and pricing from Pioneer APIs, and proxy chat completions through the OpenAI-compatible endpoint using `X-API-Key` authentication.
