---
'manifest': minor
---

Add Gemini OAuth (gemini-cli / CodeAssist flow). Sign in with a personal
Google account to route through `cloudcode-pa.googleapis.com` against
the free-tier Gemini quota. Adds a `Sign in with Google` tile on the
Routing page and a curated `gemini-2.5-pro` / `gemini-2.5-flash` /
`gemini-2.5-flash-lite` model list (the CodeAssist-supported set).
