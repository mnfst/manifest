---
"manifest": minor
---

Store agent API keys encrypted (AES-256-GCM) so users can view them anytime via a reveal/hide toggle on the Settings page, eliminating the need to rotate keys just to copy them for OpenClaw setup
