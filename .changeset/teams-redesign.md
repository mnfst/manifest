---
'manifest': minor
---

Manifest for teams (frontend): harnesses are now agents everywhere, including URLs (`/agents`, with `/harnesses` redirects kept). New Users and Projects pages with budgets, archive and per-user/per-project overviews; an Agents list built for a thousand rows with search, filters, sorting and bulk actions (projects, copy settings); owner and project filters on Overview and Requests; a fixed nine-entry sidebar; and per-model access on the agent's "Providers and models" tab. The teams endpoints are consumed through a typed client and mocked in dev builds until the backend ships them.
