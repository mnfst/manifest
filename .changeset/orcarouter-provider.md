---
'manifest': minor
---

Add OrcaRouter as a built-in provider connection: model discovery via the OrcaRouter `/v1/models` API, OpenAI-compatible routing through `https://api.orcarouter.ai/v1/chat/completions`, a dedicated `sk-orca-` API-key flow, and an OrcaRouter tile in the provider picker.
