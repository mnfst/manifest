---
'manifest': minor
---

Add gitlawb Opengateway as an OpenAI-compatible API-key provider. Users can generate a key at the gitlawb dashboard, point Manifest at it, and route requests through the gateway to upstream models (MiMo, Gemini, MiniMax, Qwen, etc.).