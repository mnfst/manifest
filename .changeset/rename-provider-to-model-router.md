---
"manifest-model-router": major
---

Rename plugin from `manifest-provider` to `manifest-model-router`. The npm package name, plugin ID, directory, and all references across CI, docs, and config have been updated.
