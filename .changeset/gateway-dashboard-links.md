---
"manifest": patch
---

Use gateway.manifest.build for cloud dashboard links and share previews. Build email image URLs from the configured dashboard URL so self-hosted installations use their own assets.

Use gateway.manifest.build for n8n API defaults and the deprecated OTLP migration message. Generate SDK examples from the current dashboard origin.

Send self-hosted pivot waitlist submissions to gateway.manifest.build and allow that origin in the dashboard content security policy.
