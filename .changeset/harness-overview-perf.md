---
'manifest': patch
---

Speed up loading the harness Overview. The Autofix KPI cards now scan the request window once for both the current and previous period instead of twice, and the page paints as soon as the main overview data is ready instead of waiting on the Autofix and model widgets.
