---
"manifest": minor
---

Store and display human-readable model names everywhere. Adds a `display_name` column to model pricing, populates it during OpenRouter sync and seeding, and uses it across all frontend pages (Overview, MessageLog, FallbackList).
