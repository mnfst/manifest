---
'manifest': patch
---

Add Cerebras as a first-class API-key provider. Manifest can now connect Cerebras keys, discover models from `https://api.cerebras.ai/v1/models`, proxy chat completions through the OpenAI-compatible endpoint, and enrich model metadata from models.dev.
