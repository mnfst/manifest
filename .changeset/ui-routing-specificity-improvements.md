---
"manifest": patch
---

Improve specificity tier UI: add empty state background, auto-open model picker on Done, show tier icons in modal, make rows clickable, fix tier name in model picker, fix reset to clear fallbacks, and pass specificity assignments to model picker for role badges
