coverage:
  status:
    project:
      default:
        target: auto
    patch:
      default:
        target: auto
flags:
  backend:
    paths:
      - packages/backend/src/
    carryforward: true
  frontend:
    paths:
      - packages/frontend/src/
    carryforward: true
  plugin:
    paths:
      - packages/openclaw-plugin/src/
    carryforward: true
comment:
  layout: "reach,diff,flags"
