# Prevent hoisting of @types/react to ensure each workspace
# uses its declared version (React 18 for manifest/frontend, React 19 for manifest-ui)
public-hoist-pattern[]=!@types/react
public-hoist-pattern[]=!@types/react-dom

# Preserve symlinks for better workspace resolution
symlink=true
